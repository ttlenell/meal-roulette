import { StyleSheet, Text, View } from "react-native";
import React from "react";

const MealListItem = () => {
  return (
    <View>
      <Text>MealListItem</Text>
    </View>
  );
};

export default MealListItem;

const styles = StyleSheet.create({});
